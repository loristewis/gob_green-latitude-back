'use strict';

/**
 * activities-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::activities-category.activities-category');
